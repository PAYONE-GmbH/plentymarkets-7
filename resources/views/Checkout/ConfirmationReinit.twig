<script>
    {% if success == "true" %}
    OffAmazonPayments.initConfirmationFlow("{{ sellerId }}", "{{ amazonReferenceId }}", function (confirmationFlow) {
        console.log('CONFIRMATION REINIT')
        confirmationFlow.success();
        doAmazonAuth();
    });
    {% else %}
    OffAmazonPayments.initConfirmationFlow("{{ sellerId }}", "{{ amazonReferenceId }}", function (confirmationFlow) {
        console.log('fail');
        confirmationFlow.error();
    });
    {% endif %}
    function doAmazonAuth () {
        console.log('AUTH')
        $.post('/payment/payone/checkout/doAuthFromOrder/{{ orderId }}', {
            },
            function (data, status) {
                console.log(data)
            });
    }

</script>
